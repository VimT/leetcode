//! 给植物浇水 II

pub fn minimum_refill(plants: Vec<i32>, capacity_a: i32, capacity_b: i32) -> i32 {}

fn main() {
    assert_eq!(minimum_refill(), );
    assert_eq!(minimum_refill(), );
    assert_eq!(minimum_refill(), );
}
